export type EndereoItem = {
    chave: string,
    valor: string | null // o valor pode ser string ou null, dependendo do retorno da API
}

export type TipoProtoco = {
    tipo:string,
     documento: string, 
     protocolo:string
    }

